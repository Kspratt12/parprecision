import type { Article } from "./articles";

// Original Next.js golf tip articles removed — WordPress-migrated versions are canonical.
// Redirects in next.config.ts point old /golf-tips/* URLs to their WP equivalents.
export const golfTipArticles: Article[] = [];
