import type { Article } from "./articles";

// Original Next.js review articles removed — WordPress-migrated versions are canonical.
// Redirects in next.config.ts point old /reviews/* URLs to their WP equivalents.
export const reviewArticles: Article[] = [];
